const fs = require('fs');
const http = require('http');
const Config = require('./config');
const ms = require('pretty-ms');
const version = '1.2.0-b';

SetConvar('Framework', `DiscordFramework‎_v${version}`);